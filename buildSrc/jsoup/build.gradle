apply plugin: "groovy"

repositories {
    mavenCentral()
}

dependencies {
    groovy localGroovy()
    compile gradleApi()
    compile 'org.jsoup:jsoup:1.6.1'
	
    runtime ('com.uwyn:jhighlight:1.0') {
        exclude module: "servlet-api"
    }

}